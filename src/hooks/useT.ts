'use client';

export { useTranslations } from 'next-intl';

/**
 * @deprecated — Use `useTranslations` from 'next-intl' directly.
 * Kept temporarily for backward compatibility during migration.
 *
 * Usage:
 *   const t = useTranslations('home');
 *   t('greeting', { name: 'Aman' })
 */
